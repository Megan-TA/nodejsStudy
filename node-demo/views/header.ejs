<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
"http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html>
    <head>
    <title><%= title %> - nodejsStudy</title>
    <link rel="stylesheet" href="/stylesheets/Normalize.css"/>
	<link rel="stylesheet" href="/stylesheets/bootstrap.css"/>
	<link rel="stylesheet" href="/stylesheets/style.css"/>
    </head>
    <body class="row">
       
       <header>
            <h1><%= title %></h1>
       </header>

        <nav class="col-md-4">
            <% if(user) { %>
                <% if(username) { %> 
                    <div class="">
                         <img src="<%= imgpath %>" alt="图片挂啦~" width="100" height="100" class="useravater-img">
                         <span><a title="上传" href="/upload">上传头像</a></span>
                         <span>欢迎您：<%= username %></span>
                    </div>
                <% } %>
              <div>
                <span><a title="主页" href="/">主页</a></span>
                <span><a title="发表" href="/post">发表</a></span>
                <span><a title="历史微博" href="/u/username=<%=username %>">历史微博</a></span>
                <span><a title="登出" href="/logout">登出</a></span>
                <div>
                    <form action="/search" method="GET">
                        <input type="text" name="keyword" placeholder="请输入要查询的微博名称" class="form-control">
                        <button type="submit" class="btn btn-default">搜索</button>
                    </form>
                </div>
              </div>
            <% } else { %>
                <span><a title="登录" href="/login">登录</a></span>
                <span><a title="注册" href="/reg">注册</a></span>
            <% } %>
        </nav>

        
        <!--主体部分-->
        <div id="content" class="col-md-8">  
            <% if(success) {%>
                <p class="bg-success"><%= success %></p> 
            <% } %>
            <% if(error) { %>
            <p class="bg-danger"><%= error %> </p>
            <% } %>
          
        
        

        